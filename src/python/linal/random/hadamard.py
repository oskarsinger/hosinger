import numpy as np

def get_hadamard_transformed(A, H=None, p=None):

    if H is None and p is None:
            raise ValueError(
                'Either H or p')

def get_hadamard(n, p):

    print "Stuff"
